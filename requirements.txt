# ---- Multi-agent orchestration ----
langgraph>=0.2.50
langchain-core>=0.3.20
langchain-openai>=0.2.10          # OpenAI-compatible client -> points at OpenRouter

# ---- Data models / config ----
pydantic>=2.7
pydantic-settings>=2.4
python-dotenv>=1.0

# ---- Scraping & collection ----
httpx>=0.27
trafilatura>=1.12                 # main-text extraction from articles/blogs
beautifulsoup4>=4.12              # HTML parsing
lxml>=5.2
ddgs>=6.0                         # free web search for startup discovery
tenacity>=9.0                     # retries / backoff

# ---- RAG with reranking ----
qdrant-client>=1.12               # local (embedded) vector database
fastembed>=0.4                    # local ONNX embeddings + local cross-encoder reranker (no torch)
rank-bm25>=0.2.2                  # lexical (BM25) search for hybrid retrieval
cohere>=5.11                      # Cohere Rerank (used only if COHERE_API_KEY is set)

# ---- Web API + dashboard ----
fastapi>=0.115
uvicorn[standard]>=0.32
jinja2>=3.1
python-multipart>=0.0.9

# ---- CLI / logging ----
rich>=13.9

# ---- Optional (auto-detected at runtime, not required) ----
# playwright>=1.48                # dynamic JS-heavy pages  ->  python -m playwright install chromium
# firecrawl-py>=1.5              # clean web extraction     ->  set FIRECRAWL_API_KEY
# psycopg[binary]>=3.2          # PostgreSQL structured store -> set DATABASE_URL=postgresql://...
